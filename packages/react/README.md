# @w5-ui/react

React component library for the Dash UI design system. Pairs with `@w5-ui/tokens` for tokens and
`@w5-ui/assets` for brand SVGs.

```tsx
import '@w5-ui/react/styles.css'
import { Card, Pill, Button, Topbar, Sidebar } from '@w5-ui/react'
```
