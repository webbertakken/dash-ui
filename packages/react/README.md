# @dash-ui/react

React component library for the Dash UI design system. Pairs with `@dash-ui/tokens` for tokens and `@dash-ui/assets` for brand SVGs.

```tsx
import '@dash-ui/react/styles.css';
import { Card, Pill, Button, Topbar, Sidebar } from '@dash-ui/react';
```
