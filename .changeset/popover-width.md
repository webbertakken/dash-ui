---
'@w5-ui/svelte': minor
---

Popover: add `width` prop (`sm` | `md` | `lg` | `xl`) to control the panel
max-width. Defaults to `sm` (320px), matching the previous behaviour. Mirrors
`Modal`'s `size` scale for content-dense panels.
