node_modules
dist
.svelte-kit
.vite
*.log
.DS_Store
.env
.env.local
.turbo

# yarn 4 caches and ephemeral install state (we use the node-modules linker,
# so yarn.lock + package.json are the source of truth; the rest can regen)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

# autoresearch
results.tsv
runs/

# session state / tracked locally only
AMBIGUITIES.md
DECISIONS.md
LESSONS_LEARNED.md
PROMPT.md
ALL_PROMPTS.md
.claude-session

# storybook output
storybook-static
*.tsbuildinfo
coverage
